user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Add WASM MIME type
    types {
        application/wasm wasm;
    }

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout  65;

    # Gzip compression
    gzip  on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml application/wasm;

    server {
        listen       80;
        listen  [::]:80;
        server_name  localhost;

        root   /usr/share/nginx/html;
        index  index.html index.htm;

        # Security headers (applied to all responses)
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # WASM headers for SharedArrayBuffer support (required by Skiko/Compose)
        # See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cross-Origin-Embedder-Policy
        add_header Cross-Origin-Opener-Policy "same-origin" always;
        add_header Cross-Origin-Embedder-Policy "require-corp" always;

        # SPA routing: fallback to index.html
        location / {
            try_files $uri $uri/ /index.html;
        }

        # index.html: NEVER cache aggressively
        # See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control
        # "no-cache" = store but revalidate before each use (allows 304 Not Modified)
        location = /index.html {
            add_header Cache-Control "no-cache" always;
            # Re-add security headers (location blocks don't inherit add_header)
            add_header X-Frame-Options "SAMEORIGIN" always;
            add_header X-Content-Type-Options "nosniff" always;
            add_header Cross-Origin-Opener-Policy "same-origin" always;
            add_header Cross-Origin-Embedder-Policy "require-corp" always;
        }

        # Entry point JS (composeApp.js) and CSS: revalidate always
        # These files don't have content hashes in their names
        location ~* ^/(composeApp\.js|styles\.css)$ {
            add_header Cache-Control "no-cache" always;
            # Re-add required headers
            add_header X-Content-Type-Options "nosniff" always;
            add_header Cross-Origin-Opener-Policy "same-origin" always;
            add_header Cross-Origin-Embedder-Policy "require-corp" always;
        }

        # Hashed JS chunks (446.js, 886.js, etc.): cache forever
        # Webpack generates these with content hashes - safe to cache aggressively
        location ~* ^/[0-9]+\.js$ {
            expires 1y;
            add_header Cache-Control "public, immutable" always;
            # Re-add required headers
            add_header X-Content-Type-Options "nosniff" always;
            add_header Cross-Origin-Opener-Policy "same-origin" always;
            add_header Cross-Origin-Embedder-Policy "require-corp" always;
        }

        # WASM files with hashes (e.g., 1c9312fc49ce6191d38b.wasm): cache forever
        # These have content hashes - safe to cache aggressively
        location ~* \.wasm$ {
            expires 1y;
            add_header Cache-Control "public, immutable" always;
            # Re-add required headers
            add_header X-Content-Type-Options "nosniff" always;
            add_header Cross-Origin-Opener-Policy "same-origin" always;
            add_header Cross-Origin-Embedder-Policy "require-corp" always;
        }

        # Static assets (images, fonts): cache long term
        location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable" always;
        }

        # Health check endpoint (for Kubernetes/Docker)
        location /health {
            access_log off;
            return 200 "OK\n";
            add_header Content-Type text/plain;
        }

        # Error pages
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}
